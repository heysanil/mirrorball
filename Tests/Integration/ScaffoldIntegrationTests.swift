import Testing
@testable import MirrorballSwift

@Suite("Scaffold integration")
struct ScaffoldIntegrationTests {
    @Test("Integration target builds")
    func builds() {
        // Placeholder. Replaced by TunnelSupervisor integration tests driven by
        // the fake-ssh harness.
        #expect(Bool(true))
    }
}
